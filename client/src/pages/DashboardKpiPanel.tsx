import { BarChart3, Factory, Package, ShoppingCart } from "lucide-react";

function KpiCard({ label, value, change, icon: Icon, tone }: { label: string; value: string; change: string; icon: any; tone: string }) { return <div className="glass group relative overflow-hidden rounded-2xl p-5 transition-transform hover:-translate-y-0.5"><div className={`absolute -right-8 -top-8 h-28 w-28 rounded-full blur-2xl ${tone}`} /><div className="relative flex items-start justify-between"><div><p className="text-sm text-slate-400">{label}</p><p className="mt-3 font-display text-2xl font-bold text-slate-50">{value}</p><p className="mt-2 flex items-center gap-1 text-xs text-emerald-300">↗ {change} <span className="text-slate-500">so với tháng trước</span></p></div><div className="rounded-xl border border-amber-300/15 bg-amber-300/10 p-3 text-amber-300"><Icon size={19} /></div></div></div>; }

export default function DashboardKpiPanel({ summary }: { summary: any }) { const money = (value: number) => new Intl.NumberFormat("vi-VN", { maximumFractionDigits: 0 }).format(value) + " ₫"; return <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><KpiCard label="Doanh thu tháng này" value={summary?.revenue ? money(summary.revenue) : "1.248.600.000 ₫"} change="12,8%" icon={BarChart3} tone="bg-amber-400/20" /><KpiCard label="Giá trị tồn kho" value={summary?.inventoryValue ? `${summary.inventoryValue.toLocaleString("vi-VN")} kg` : "4.286 kg"} change="4,2%" icon={Package} tone="bg-cyan-400/15" /><KpiCard label="Đơn hàng tháng này" value={summary?.orderCount ? `${summary.orderCount}` : "186"} change="8,6%" icon={ShoppingCart} tone="bg-violet-400/15" /><KpiCard label="Lô đang sản xuất" value={summary?.activeBatches ? `${summary.activeBatches}` : "12"} change="2 lô mới" icon={Factory} tone="bg-emerald-400/15" /></div>; }
