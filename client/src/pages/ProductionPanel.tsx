import { Factory } from "lucide-react";
import { trpc } from "@/lib/trpc";

export default function ProductionPanel() {
  const production = trpc.production.list.useQuery();
  const label = (status: string) => status === "completed" ? "Hoàn thành" : status === "in_progress" ? "Đang chạy" : "Đã lập kế hoạch";
  return <section><div className="mb-6"><p className="text-xs uppercase tracking-[.2em] text-amber-300/70">VẬN HÀNH / SẢN XUẤT</p><h2 className="mt-1 flex items-center gap-3 font-display text-3xl font-bold"><Factory className="text-amber-300" size={27} />Quá trình nấu bia</h2><p className="mt-2 text-sm text-slate-500">Theo dõi tiến độ, trạng thái và ngày khởi tạo của từng lô sản xuất.</p></div><div className="glass overflow-x-auto rounded-2xl"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b border-white/10 text-[10px] uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-3">Mã lô</th><th className="px-5 py-3">Loại bia</th><th className="px-5 py-3">Tiến độ</th><th className="px-5 py-3">Ngày bắt đầu</th><th className="px-5 py-3">Trạng thái</th></tr></thead><tbody>{production.isLoading ? <tr><td colSpan={5} className="px-5 py-10 text-center text-slate-500">Đang tải lô sản xuất...</td></tr> : (production.data ?? []).map((item: any) => <tr key={item.id} className="border-t border-white/5"><td className="px-5 py-4 font-semibold text-amber-200">{item.batchCode}</td><td className="px-5 py-4 text-slate-300">BEER#{item.beerTypeId}</td><td className="px-5 py-4 text-slate-300">{item.status === "completed" ? "100%" : item.status === "in_progress" ? "68%" : "0%"}</td><td className="px-5 py-4 text-slate-400">{item.createdAt ? new Date(item.createdAt).toLocaleDateString("vi-VN") : "-"}</td><td className={`px-5 py-4 ${item.status === "completed" ? "text-teal-200" : "text-amber-200"}`}>{label(item.status)}</td></tr>)}</tbody></table></div></section>;
}
